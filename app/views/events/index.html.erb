<h3>Events here:</h3>
<%= form_tag(new_search_path, method: "get") do %>
  <div class="form-row">
    <div class="form-group col-sm-5">
      <label for="name">Name:</label>
      <%= text_field_tag :description, params[:description], placeholder: "Name" %>
    </div>

    <div class="form-group col-md-6">
      <%= text_field_tag :description, params[:description], placeholder: "Artist" %>
    </div>

    <div class="form-group col-md-6">
      <label for="type">Type:</label>
      <%= text_field_tag :type, params[:type], placeholder: "Type" %>
    </div>

    <%= submit_tag "Search", name: nil %>
  </div>
<% end %>