<table class="table table-hover table-bordered">
  <thead>
    <tr>
      <th><%= I18n.t('rails_watcher.events.id') %></th>
      <th><%= I18n.t('rails_watcher.events.title') %></th>
      <th><%= I18n.t('rails_watcher.events.status') %></th>
      <th><%= I18n.t('rails_watcher.events.type') %></th>
      <th><%= I18n.t('rails_watcher.events.user_login') %></th>
      <th><%= I18n.t('rails_watcher.events.user_ip') %></th>
      <th><%= I18n.t('rails_watcher.navigation.actions') %></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.id %><td>
        <td><%= event.title %><td>
        <td><%= event.status %><td>
        <td><%= event.type %><td>
        <td><%= event.user_login %><td>
        <td><%= event.ip %><td>
        <td><%= link_to I18n.t('rails_watcher.navigation.details'), event %><td>
      </tr>
    <% end %>
  </tbody>
</table>
